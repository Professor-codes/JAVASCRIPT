// arr.map((value, index, array) => {
//      console.log(value, index, array);
// })
// create a new array by performing operations on existing elements of an array

let numbers = [1, 2, 3, 4, 5];
let square = numbers.map((i) => {
    return i * i;
})
console.log(square)

